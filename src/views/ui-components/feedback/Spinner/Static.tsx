import React from "react";
import { Spinner } from "components/ui";

const Static = () => {
  return (
    <div>
      <Spinner isSpinning={false} size="40px" />
    </div>
  );
};

export default Static;
